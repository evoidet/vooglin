# Vooglin

Minimal website for Vooglin automation and digital workflow services.

## Preview locally

```bash
python3 -m http.server 8000
```

Open `http://localhost:8000`. The pricing and privacy pages are available at `http://localhost:8000/pricing/` and `http://localhost:8000/privacy/`.

## Build

```bash
npm run build
```

The build packages both pages, the favicon and the social preview image into a deployment-ready worker.

## Edit the content

- Homepage structure and public copy are in `index.html`.
- Pricing structure and copy are in `pricing/index.html`.
- Privacy Policy structure and English source copy are in `privacy/index.html`; Estonian and Russian versions are generated by `localize.mjs`.
- Visual styling and responsive layouts are in `styles.css`.
- The current year, mobile navigation, calculator, counters and collaborations are handled in `script.js`.
- Real homepage statistics and future collaboration entries are maintained in `site-config.js`.
- Put future collaboration logo files in `images/partners/`, then reference them as `/images/partners/filename.svg` in `site-config.js`.
- The founder section contains a 4:5 portrait placeholder that can be replaced when the final photo is available.
- The Vooglin convergence mark is built in HTML/CSS through the `.brand-symbol` class.
- `favicon.png` and `og-brand.png` are generated by `scripts/render-brand-assets.mjs`; `og-savings.png` is the current social preview card.
