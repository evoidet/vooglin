/*
 * Public homepage data.
 *
 * Update real statistics here when they are ready. Values at 0 stay hidden.
 * Add future collaborators to the array below; the whole section stays hidden
 * while the array is empty.
 */
window.vooglinSiteConfig = Object.freeze({
  stats: Object.freeze({
    organisations: 0,
    automations: 0,
    workflows: 0,
    estimatedHoursSaved: 0,
  }),
  collaborations: Object.freeze([
    // {
    //   name: "Example Organisation",
    //   logo: "/images/partners/example.svg",
    //   url: "https://example.com",
    //   description: "Community partner",
    // },
  ]),
});
