{
  "name": "vooglin-site",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build-site.mjs",
    "test": "npm run build && node --check dist/server/index.js"
  }
}
